{
  "idmHost": "${env.XIVELY_IDM_HOST}",
  "timeSeriesHost": "${env.XIVELY_TIMESERIES_HOST}",
  "brokerHost": "${env.XIVELY_BROKER_HOST}",
  "brokerPort": "${env.XIVELY_BROKER_WS_PORT}",
  "blueprintHost": "${env.XIVELY_BLUEPRINT_HOST}",
  "accountId": "${env.XIVELY_ACCOUNT_ID}",
  "emailAddress": "${env.XIVELY_ACCOUNT_USER_NAME}",
  "password": "${env.XIVELY_ACCOUNT_USER_PASSWORD}",
  "organizationId": "${organization.id}",
  "airnow": {
    "apikey": "640AA087-B3E7-4098-B3F3-9F8EF81C0BC7",
    "boundingbox": "-71.059289,42.335449,-71.017232,42.365642"
  },
  "user": {
    "username": "${endUser.id}",
    "password": "${mqttUser.secret}"
  },
  "device": {
    "id": "${device.id}",
    "accountId": "${device.accountId}",
    "mqtt": {
      "username": "${env.XIVELY_ACCOUNT_BROKER_USER}",
      "password": "${env.XIVELY_ACCOUNT_BROKER_PASSWORD}"
    },
    "channelnamemap": {
      "control": "control",
      "sensor": "sensor",
      "deviceinfo": "device-log"
    }
  }
}
