{
  "userInfo": {
    "user": "${env.XIVELY_ACCOUNT_BROKER_USER}",
    "password": "${env.XIVELY_ACCOUNT_BROKER_PASSWORD}"
  },
  "mqtt": {
    "mqttBroker": "${env.XIVELY_BROKER_HOST}",
    "mqttPort": "${env.XIVELY_BROKER_PORT}",
    "mqttWsPort": "${env.XIVELY_BROKER_WS_PORT}",
    "mqttUseSSL": true,
    "deviceId": "${device[0].id}",
    "mqttDataChannel": "${_.find(device[0].channels, 'channelTemplateName', 'sensor').channel}",
    "mqttControlChannel": "${_.find(device[0].channels, 'channelTemplateName', 'control').channel}",
    "mqttDeviceDataChannel": "${_.find(device[0].channels, 'channelTemplateName', 'device-log').channel}"
  }
}
